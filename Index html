<!doctype html>
<html lang="hi">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Document Scanner</title>
<style>
  :root{--bg:#0f1724;--card:#0b1320;--accent:#06b6d4;--muted:#9aa7b2}
  body{margin:0;font-family:Inter,system-ui,Segoe UI,Roboto,"Helvetica Neue",Arial;background:var(--bg);color:#e6eef6;display:flex;min-height:100vh;align-items:flex-start;gap:16px;padding:18px;box-sizing:border-box}
  .app{max-width:1100px;margin:auto;width:100%;display:grid;grid-template-columns:1fr 420px;gap:18px}
  .panel{background:linear-gradient(180deg,#07101a 0%, #071827 100%);border-radius:12px;padding:12px;box-shadow:0 6px 24px rgba(2,6,23,0.6)}
  .camera {position:relative;min-height:420px;display:flex;flex-direction:column;align-items:center;justify-content:center;overflow:hidden;border-radius:8px}
  video, canvas{max-width:100%;border-radius:8px}
  .controls{display:flex;gap:8px;margin-top:10px;flex-wrap:wrap}
  button{background:var(--accent);color:#052025;border:0;padding:8px 12px;border-radius:8px;font-weight:600;cursor:pointer}
  .secondary{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted)}
  .small{padding:6px 8px;font-size:14px}
  .right{display:flex;flex-direction:column;gap:12px}
  label{font-size:13px;color:var(--muted)}
  .preview{background:#071224;padding:8px;border-radius:8px;min-height:220px;display:flex;align-items:center;justify-content:center;flex-direction:column;gap:8px}
  .corners{position:relative;width:100%;height:320px;border-radius:6px;overflow:hidden;background:#051018}
  .corner-dot{position:absolute;width:14px;height:14px;border-radius:50%;background:var(--accent);transform
